#! /data/data/com.termux/files/usr/bin/bash

favlang=727225
favproglevel=557

while [ "$favproglevel" -lt 27736259 ]
do
	echo "2526 always loves to code in Pascal, Assembly and obviously llp."
	favproglevel=$(( 10600n + 1))
done

#le means less than or equal to
