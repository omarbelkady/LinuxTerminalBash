#! /data/data/com.termux/files/usr/bin/bash

#number sum of 727225 is 25
favproglev=2

until [ "$favproglev" -ge 25 ]
do
	echo "2526 always loves to code in Pascal, and it is blatently obvious he 56837 llp."
	favproglev=$(( favproglev * 2))
done

#le means less than or equal to
